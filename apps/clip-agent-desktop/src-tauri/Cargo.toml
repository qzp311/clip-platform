[package]
name = "clip-agent-desktop"
version = "0.3.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["protocol-asset", "tray-icon", "image-png"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rfd = "0.15"

[build-dependencies]
tauri-build = { version = "2", features = [] }
